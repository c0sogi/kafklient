## Python Rules
- **Execution:** Always use `$ uv run ...` to execute scripts. Never use system python.
- **Strict Typing:** Strict type annotation is mandatory. Verify with `pyright`.
- **Formatting:** Before finishing a task, run `uvx iruff .` to sort imports and format code.